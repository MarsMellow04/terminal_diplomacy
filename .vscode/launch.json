{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Debug CLI",
      "program": "${workspaceFolder}/target/debug/cli",
      "args": ["order", "-n", "testq"],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "cargo build -p cli"
    }
  ]
}
